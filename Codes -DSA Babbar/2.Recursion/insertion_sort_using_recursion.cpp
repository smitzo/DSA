#include <iostream>
using namespace std;

void insertionsort(int arr[], int size)
{
    if (size <= 0)
    {
        return ;
    }
    if (size = 1)
    {
        
    }

        if ( arr[size -1]>arr[size -2])
        {



        }

        
        else
        {
            insertionsort(arr, size - 1);
        }
}
int main()
{
    int array1[5] = {3, 5, 1, 23, 2};
    insertionsort(array1,5)
}